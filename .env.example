VITE_API_BASE_URL=http://13.159.7.199:5001

# On Vercel: set the same variable in Project → Settings → Environment Variables,
# then redeploy. Required so production login hits the API (not your-app.vercel.app/api/...).
