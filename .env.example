# Used by the Vite dev proxy (and optional direct mode).
VITE_API_BASE_URL=http://13.159.7.199:5001

# Leave unset on Vercel. The app calls same-origin `/api/...` and vercel.json
# proxies that to the API host (avoids 405 on POST /api and mixed-content issues).
# Set VITE_API_DIRECT=true only if you intentionally bypass the proxy.
